<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- Log message format -->
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] ${APPLICATION_NAME} %-5level %logger{36} - %msg%n
      </pattern>
    </encoder>
  </appender>

  <!-- Need appender to write to file -->
  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>/var/app_log/${APPLICATION_NAME}.log</file>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] ${APPLICATION_NAME} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Setting the com.lavkesh.cloud level of logging to INFO -->
  <logger name="com.lavkesh.cloud" level="debug">
    <appender-ref ref="FILE" />
  </logger>

  <!-- Setting the root level of logging to INFO -->
  <root level="info">
    <appender-ref ref="STDOUT" />
    <appender-ref ref="FILE" />
  </root>
</configuration>